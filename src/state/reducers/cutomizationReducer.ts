import { Action } from "../actions";

interface CustomizationReducer{

}
const INIT_STATE: CustomizationReducer = {
    
}
const reducer = (state: CustomizationReducer = INIT_STATE, action: Action)=>{

    return state;
}

export default reducer;